name: on-push action

on:
  push:
    branches: [main]

jobs:
  UV:
    runs-on: ubuntu-latest
    steps:
      - name: fails but continues
        run: exit 1
        continue-on-error: true
      - name: passes
        run: exit 0
