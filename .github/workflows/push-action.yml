name: on-push action

on:
  push:
    branches: [main]

jobs:
  UV:
    runs-on: ubuntu-latest
    steps:
      - name: fails but continues
        id: coe
        run: exit 1
        continue-on-error: true
      - name: passes
        run: echo "${{ steps.coe.outcome }}"
