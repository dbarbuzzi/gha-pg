name: "on push"

on: [push]

jobs:
  INTERPOLATION-CHECK:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/interps
        with:
          input-one: true
          input-two: false
