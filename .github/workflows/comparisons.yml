name: checking out comparisons

on: [push]

jobs:
  COMPARISONS:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: outputs
        uses: ./.github/actions/outputs

      - name: 'if: ${{ steps.outputs.outputs.zero }}'
        if: ${{ steps.outputs.outputs.zero }}
        run: echo "hi!"
      - name: 'if: ${{ steps.outputs.outputs.one }}'
        if: ${{ steps.outputs.outputs.one }}
        run: echo "hi!"
